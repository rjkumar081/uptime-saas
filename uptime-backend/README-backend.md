1) Install Docker & Node.js
2) Start local infra:
   docker compose up -d
3) Copy .env.example -> .env and fill keys
4) npm install
5) npm run migrate
6) npm start
7) npm run worker   (in another terminal)
